---

  - name: Update apt cache
    apt:
      update_cache: yes
    become: yes

  - name: Install applications with apt
    apt:
      name: '{{ item }}'
      state: present
    loop: "{{ apt }}"
    become: yes
