---

   - name: Create ~/.config directories
     file: path={{ item }} state=directory
     with_items:
         - "{{ CONFIG_DIRS }}"

   - name: Create ~/src directories
     file: path={{ item }} state=directory
     with_items:
         - "{{ SRC_DIRS }}"
